easyblock = 'CMakeMake'

name = 'LLVM'
version = '3.8.1'

homepage = 'http://llvm.org/'
description = """The LLVM Core libraries provide a modern source- and target-independent
 optimizer, along with code generation support for many popular CPUs
 (as well as some less common ones!) These libraries are built around a well
 specified code representation known as the LLVM intermediate representation
 ("LLVM IR"). The LLVM Core libraries are well documented, and it is
 particularly easy to invent your own language (or port an existing compiler)
 to use LLVM as an optimizer and code generator."""

toolchain = {'name': 'CrayGNU', 'version': '18.08'}
toolchainopts = {'cstd': 'gnu++11'}

source_urls = ['http://%(namelower)s.org/releases/%(version)s']
sources = ['%(namelower)s-%(version)s.src.tar.xz']

builddependencies = [
    ('CMake', '3.6.0'),
    ('cray-python/2.7.15.1', EXTERNAL_MODULE),
]
dependencies = [
    ('ncurses', '6.1'),
    ('zlib', '1.2.11'),
]

configopts = '-DBUILD_SHARED_LIBS=ON -DCMAKE_EXE_LINKER_FLAGS="$LDFLAGS" -DLLVM_INSTALL_UTILS=ON -DLLVM_TARGETS_TO_BUILD=X86 -DLLVM_ENABLE_ZLIB=ON'

separate_build_dir = True

sanity_check_paths = {
    'files': ['bin/%(namelower)s-ar', 'bin/FileCheck'],
    'dirs': ['include/%(namelower)s', 'include/%(namelower)s-c'],
}

moduleclass = 'compiler'
